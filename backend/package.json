{
  "name": "backend",
  "version": "1.0.0",
  "description": "generated by cofounder backend layer",
  "author": "n@raiden.ai",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node initdb && nodemon server",
    "build": "node initdb.js",
    "preinstall": "npm cache clean --force && npm cache verify",
    "prepare": "npm set fetch-retries 5 && npm set fetch-retry-mintimeout 100000 && npm set fetch-retry-maxtimeout 600000 && npm set network-timeout 300000 && npm ci --prefer-offline"
  },
  "config": {
    "fetch-retries": 5,
    "fetch-retry-mintimeout": 100000,
    "fetch-retry-maxtimeout": 600000,
    "network-timeout": 300000
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@electric-sql/pglite": "^0.2.4",
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.3",
    "uuid": "*",
    "yaml": "^2.5.0",
    "morgan": "^1.10.0",
    "bcrypt": "*"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.4"
  }
}